<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.generated.ldesportsbar.mapper.NotificationMapper">

  <resultMap id="NotificationResultMap" type="com.generated.ldesportsbar.model.Notification">
    <id property="id" column="id"/>
    <result property="recipientRole" column="recipient_role"/>
    <result property="recipientName" column="recipient_name"/>
    <result property="title" column="title"/>
    <result property="content" column="content"/>
    <result property="notificationType" column="notification_type"/>
    <result property="relatedOrderId" column="related_order_id"/>
    <result property="isRead" column="is_read"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <select id="findByRecipientRole" resultMap="NotificationResultMap">
    SELECT * FROM notifications WHERE recipient_role = #{role} ORDER BY created_at DESC
  </select>

  <select id="findUnreadByRecipientRole" resultMap="NotificationResultMap">
    SELECT * FROM notifications WHERE recipient_role = #{role} AND is_read = 0 ORDER BY created_at DESC
  </select>

  <select id="findByType" resultMap="NotificationResultMap">
    SELECT * FROM notifications WHERE notification_type = #{type} ORDER BY created_at DESC
  </select>

  <select id="findById" resultMap="NotificationResultMap">
    SELECT * FROM notifications WHERE id = #{id}
  </select>

  <select id="countUnreadByRecipientRole" resultType="long">
    SELECT COUNT(*) FROM notifications WHERE recipient_role = #{role} AND is_read = 0
  </select>

  <insert id="insert" parameterType="com.generated.ldesportsbar.model.Notification" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO notifications (
      recipient_role, recipient_name, title, content, notification_type, related_order_id, is_read
    ) VALUES (
      #{recipientRole}, #{recipientName}, #{title}, #{content}, #{notificationType}, #{relatedOrderId}, #{isRead}
    )
  </insert>

  <update id="markAsRead">
    UPDATE notifications SET is_read = 1 WHERE id = #{id}
  </update>

  <update id="markAllAsRead">
    UPDATE notifications SET is_read = 1 WHERE recipient_role = #{role} AND is_read = 0
  </update>

</mapper>
